export const ItemTypes = Object.freeze({
  YOUTUBE: 'youtube',
  DEVICE: 'device',
  DRIVE: 'drive',
  NOTEBOOK: 'notebook',
  NOTE: 'note',
});
export const ScreenTypes = Object.freeze({
  MAIN: 'out',
  IN: 'in',
});
